const btn = addEventListener('click', function(){
	const width = window.outerHeight = window.screen.availWidth;
	const height = window.outerHeight = window.screen.availHeight;
	alert(`Размер вашего экрана по ширине составляет ${width} px и по высоте - ${height} px.`);
})
